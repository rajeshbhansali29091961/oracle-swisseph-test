[project]
name = "bhoovalaya-oracle-test"
version = "0.1.0"
description = "Swiss Ephemeris native-lib test on Flet/Android"
requires-python = ">=3.10"
dependencies = [
    "flet",
    "swisseph-ffi",
]

[tool.flet]
org = "com.rajesh.oracle"
product = "Bhoovalaya Oracle Test"

[tool.flet.android]
